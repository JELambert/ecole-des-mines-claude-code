[project]
name = "ecole-des-mines-dashboard"
version = "0.1.0"
description = "Streamlit demo dashboard for the École des Mines guest lecture (2026-05-22)."
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40",
    "pandas>=2.2",
    "numpy>=1.26",
    "plotly>=5.24",
    "openpyxl>=3.1",
    "pyarrow>=17.0",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
